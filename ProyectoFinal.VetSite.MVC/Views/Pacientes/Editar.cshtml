@model ProyectoFinal.Entidades.Paciente

@{
    ViewBag.Title = "Editar un paciente";
}

<section class="page-section">
    <div class="page-section-header">
        <div>
            <p class="page-section-kicker">Actualización</p>
            <h2 class="page-section-title">@ViewBag.Title</h2>
            <p class="page-section-copy">Ajusta la información principal del paciente sin salir del flujo clínico.</p>
        </div>
    </div>

    <form asp-action="Editar" method="post" class="form-shell">
        <partial name="_Formulario" model="Model" />
    </form>
</section>
