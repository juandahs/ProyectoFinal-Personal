@model ProyectoFinal.Entidades.Usuario

@{
    ViewBag.Title = "Editar un usuario";
}

<h1>@ViewBag.Title</h1>

<form asp-action="Editar" method="post">
    <div class="card">
        <div class="card-body">
            <partial name="_Formulario" model="Model" />
        </div>
    </div>
</form>


<partial name="_CambiarClave" model="Model" />
